
TODO:

refresh buttons @ spectrometer and comport
save all button
restart thread on switch spectrometer
add movement to first position to est. time
connect in own thread
known bugs:
temperature does not get set (sometimes)

code:

why is plotting (kind of ) slow, maybe switch to QGraph?
thread wrapper class
rename buttons
rework structure of measurement dummy